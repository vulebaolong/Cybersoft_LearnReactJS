const initState = {
    number: 1,
};

export const numberReducer = (state = initState, action) => {
    switch (action.type) {
        // case value:
        //     break;

        default:
            return state;
    }
};
